# ⚠️ DEPRECATED: This App Service deployment has been replaced with Container Apps
# 
# This file is kept for reference but is no longer active.
# 
# New deployment pipelines:
# - .github/workflows/preprod-pipeline.yml (for test branch)
# - .github/workflows/prod-pipeline.yml (for main branch)
#
# Migration benefits:
# - Better regional availability
# - Fewer region restrictions
# - More flexible scaling
# - Modern Kubernetes-based architecture
#
# To migrate:
# 1. Use the new Container App pipelines
# 2. Update your Azure Container Registry name in the new pipelines
# 3. Configure your environment variables
# 4. Deploy using the new workflows

name: 🚫 DEPRECATED - App Service Deployment

# This workflow is disabled
on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type "DEPRECATED" to confirm you want to run this old workflow'
        required: true
        type: string

# This workflow will not run automatically
# Use the new Container App pipelines instead
